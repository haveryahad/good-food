import React from "react";

class UserGrandChild extends React.Component {
  constructor(props) {
    super(props);

    // Render Phase: ${this.props.componentType} Constructor
  }

  componentDidMount() {
    // Commit Phase: ${this.props.componentType} Component Did Mount
  }

  render() {
    // Render Phase: ${this.props.componentType} Render
  }
}

export default UserGrandChild;
